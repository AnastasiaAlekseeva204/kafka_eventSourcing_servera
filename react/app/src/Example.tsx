/*import { Card, CardActions, CardContent, CardHeader, Collapse, Typography } from "@mui/material"
import { useState } from "react"

function Example() {
  const [collapse, setCollapse] = useState(false)
  return <>
  <Card>
    <CardHeader title = "Card" subheader = "подзаголовок" ></CardHeader>
    <CardContent>
      Содержимое
    </CardContent>
    <CardActions>
      <button onClick={()=>setCollapse(collapse => !collapse)}>Кнопка</button>
    </CardActions>
    <Collapse in={!collapse}>
    Скрытое содержимое
    </Collapse>
  </Card>
  </>
}

export default Example
*/